future:
- add BDD with SpecFlow

in progress:
- make a basic nUnit framework
    * should add MORE sort assertions to the CustomDriver
    * implement MORE basic driver methods to CustomDriver with sort assertions so they can be used directly instead of instance.driver.method

optional:
- add docker support